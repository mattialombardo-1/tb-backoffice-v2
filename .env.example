# Copia in `.env.local` per puntare al backend vero.
# Per il prototipo con dati finti non serve: usa `npm run dev:mock`.

# AdminApiStackStg su AWS API Gateway.
VITE_API_BASE_URL=https://tw16nt2svf.execute-api.eu-south-1.amazonaws.com/prod

# SSO Cognito (Peerpetual).
VITE_SSO_AUTHORITY=https://sso.peerpetual.com
VITE_COGNITO_CLIENT_ID=19eekkg0921ktg7nh24ii72pcn

# Prefisso host del simulatore: `stg-` per staging, vuoto per produzione.
VITE_SIMULATOR_PREFIX=stg-
